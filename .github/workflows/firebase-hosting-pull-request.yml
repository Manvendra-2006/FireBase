# # This file was auto-generated by the Firebase CLI
# # https://github.com/firebase/firebase-tools

# name: Deploy to Firebase Hosting

# on:
#   push:
#     branches:
#       - main

#   pull_request:

# permissions:
#   checks: write
#   contents: read
#   pull-requests: write

# jobs:
#   build_and_preview:
#     if: ${{ github.event_name == 'push' || github.event.pull_request.head.repo.full_name == github.repository }}
#     runs-on: ubuntu-latest

#     steps:
#       - uses: actions/checkout@v4

#       - run: npm install

#       - run: npm run build

#       - uses: FirebaseExtended/action-hosting-deploy@v0
#         with:
#           repoToken: ${{ secrets.GITHUB_TOKEN }}
#           firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_MINI_PROJECT_7ECFD }}
#           projectId: mini-project-7ecfd


# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting

on:
  push:
    branches:
      - main

  pull_request:

permissions:
  checks: write
  contents: read
  pull-requests: write

jobs:
  build_and_preview:
    if: ${{ github.event_name == 'push' || github.event.pull_request.head.repo.full_name == github.repository }}
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - run: npm install
        working-directory: Project/project

      - run: npm run build
        working-directory: Project/project

      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_MINI_PROJECT_7ECFD }}
          projectId: mini-project-7ecfd
          entryPoint: Project/project
          channelId: live
           